#include <bits/stdc++.h>
using namespace std;
int n,x[101],y[101],P;

int solve(){
  int res=0,used[101]={};
  for(int i=0;i<n;i++) if9

  for(int i=0;i<n;i++){

    int mn = 1e9,idx=0;    
    for(int j=0;j<n;j++){
      if(mn>
    }

  }
}

int main(){
  cin>>n>>P;
  for(int i=0;i<n;i++)cin>>x[i]>>y[i];
  

  return 0;
}
